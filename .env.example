# Copy to .env for local dev. All vars are optional — defaults in src/eve_mcp/config.py.

# Path to the SQLite SDE (run scripts/fetch_sde.py to populate)
EVE_MCP_SDE_PATH=data/sde.sqlite

# Path to the ESI price cache (created automatically)
EVE_MCP_PRICE_CACHE_PATH=data/price_cache.sqlite

# How long a cached price is considered fresh (seconds)
EVE_MCP_PRICE_CACHE_TTL_SECONDS=1800

# ESI base URL and region ID for market lookups (default: The Forge / Jita)
EVE_MCP_ESI_BASE_URL=https://esi.evetech.net/latest
EVE_MCP_ESI_MARKET_REGION_ID=10000002

# Bind address for the MCP server
EVE_MCP_HOST=0.0.0.0
EVE_MCP_PORT=8080
